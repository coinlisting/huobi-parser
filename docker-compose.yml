version: "3.9"
services:

  news_cryptobot:
    restart: always
    container_name: news_cryptobot
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./logs:/news/logs
      - ./database/files:/news/database/files